<%= form_with(model: stuffing, local: true) do |form| %>
  <% if stuffing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stuffing.errors.count, "error") %> prohibited this stuffing from being saved:</h2>

      <ul>
        <% stuffing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :flavor %>
    <%= form.text_field :flavor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
